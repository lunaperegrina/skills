{
  "skill_name": "figma-pixel-perfect",
  "evals": [
    {
      "id": 0,
      "eval_name": "terse-slash-prompt",
      "prompt": "/figma-pixel-perfect https://www.figma.com/design/Q0Kt1wKVS1PW8aNs8vuCMb \"Pricing\"",
      "expected_output": "The static pricing page in the fixture repo is fixed to match the Figma 'Pricing' frame: hero title 48px, subtitle uses secondary text token (#6b7280), cards gain 1px #e5e7eb border, 16px radius and 32px padding, price 40px, buttons 8px radius with primary token background (#4f46e5) and copy 'Get started'. Outputs contain a final rendered screenshot and a report of changed files.",
      "files": [],
      "assertions": [
        "Hero title font-size is 48px (was 32px)",
        "Hero subtitle color uses the secondary text token (#6b7280), not hardcoded #999999",
        "Cards have a 1px border using the border token/color #e5e7eb (was missing)",
        "Card border-radius is 16px / --radius-card (was 4px)",
        "Card padding is 32px (was 20px)",
        "Price font-size is 40px (was 32px)",
        "Button border-radius is 8px / --radius-button (was 999px pill)",
        "Button background uses the primary token #4f46e5 (was hardcoded #6c5ce7)",
        "Button copy is 'Get started' (was 'Buy now') on all three cards",
        "A rendered screenshot of the page exists in outputs",
        "No extraneous files added to the repo (no PRD/spec/asset files)",
        "report.md exists in outputs and lists the changed files"
      ]
    },
    {
      "id": 1,
      "eval_name": "casual-pt-prompt",
      "prompt": "nossa, a página de pricing aqui no projeto tá bem diferente do figma do time :( o design é esse aqui: https://www.figma.com/design/Q0Kt1wKVS1PW8aNs8vuCMb — a tela se chama \"Pricing\". pode deixar a página igual ao design?",
      "expected_output": "Same set of visual fixes as the terse prompt, derived from a casual Portuguese request without the slash command: tokens reused instead of hardcoded colors, corrected sizes/radii/border/padding, 'Get started' button copy, final screenshot captured, report written.",
      "files": [],
      "assertions": [
        "Hero title font-size is 48px (was 32px)",
        "Hero subtitle color uses the secondary text token (#6b7280), not hardcoded #999999",
        "Cards have a 1px border using the border token/color #e5e7eb (was missing)",
        "Card border-radius is 16px / --radius-card (was 4px)",
        "Card padding is 32px (was 20px)",
        "Price font-size is 40px (was 32px)",
        "Button border-radius is 8px / --radius-button (was 999px pill)",
        "Button background uses the primary token #4f46e5 (was hardcoded #6c5ce7)",
        "Button copy is 'Get started' (was 'Buy now') on all three cards",
        "A rendered screenshot of the page exists in outputs",
        "No extraneous files added to the repo (no PRD/spec/asset files)",
        "report.md exists in outputs and lists the changed files"
      ]
    }
  ]
}
